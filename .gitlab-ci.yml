stages:
  - build
  - test
  - docker
  - deploy

variables:
  GIT_DEPTH: 0
  MAVEN_CLI_OPTS: "--batch-mode"
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"
  DOCKER_TAG: $CI_COMMIT_SHORT_SHA


cache:
  paths:
    - .m2/repository


####################
# 📊 Run SonarQube test analysis
####################
build_and_test:
  image: maven:3.9-eclipse-temurin-17
  stage: build
  tags:
    - payment
  script:
    - echo "📁 当前目录：" && pwd && ls -al
    - echo "🔨 使用父模块构建所有服务"
    - mvn $MAVEN_CLI_OPTS clean install

######################
# 📊 SonarQube Analysis (after build)
######################
test:
  image: maven:3.9-eclipse-temurin-17
  stage: test
  tags:
    - payment

  script:
    - echo "📊 Rebuilding before Sonar analysis"
    - mvn $MAVEN_CLI_OPTS clean install -DskipTests
    - echo "📊 Running SonarQube analysis"
    - mvn $MAVEN_CLI_OPTS sonar:sonar -Dsonar.projectKey=order-payment -Dsonar.host.url=$SONAR_HOST_URL -Dsonar.login=$SONAR_TOKEN


  only:
    - merge_requests
    - main
    - develop

# 🐳 Docker 构建 & 推送
####################
docker_build_push:
  image: docker:24.0.7
  stage: docker
  tags:
    - payment
  services:
    - docker:24.0.7-dind
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
  script:
    - echo "🔐 Logging into Docker registry"
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin $DOCKER_REGISTRY
    - echo "🐳 Building & pushing images with tag $DOCKER_TAG"
    - for dir in Authentication-8083 Gateway-9090 Order-8082 Payment-8081 Product-8080; do
      IMAGE_NAME=$(echo $dir | tr '[:upper:]' '[:lower:]') ;
      docker build -t $DOCKER_REGISTRY/$DOCKER_NAMESPACE/$IMAGE_NAME:$DOCKER_TAG $dir ;
      docker push $DOCKER_REGISTRY/$DOCKER_NAMESPACE/$IMAGE_NAME:$DOCKER_TAG ;
      done
  only:
    - main
####################
# 🚀 远程部署
####################
deploy_to_server:
  image: alpine:latest
  stage: deploy
  tags:
    - payment
  before_script:
    - apk add --no-cache openssh
    - mkdir -p ~/.ssh
    - echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
    - chmod 600 ~/.ssh/id_rsa
    - ssh-keyscan -H $DEPLOY_HOST >> ~/.ssh/known_hosts
  script:
    - echo "📦 Generating .env"
    - echo "IMAGE_TAG=$CI_COMMIT_SHORT_SHA" > Docker/.env

    - echo "📤 Sending files to server"
    - scp Docker/docker-compose.yml $DEPLOY_USER@$DEPLOY_HOST:/home/$DEPLOY_USER/app/
    - scp Docker/.env $DEPLOY_USER@$DEPLOY_HOST:/home/$DEPLOY_USER/app/

    - echo "🚀 Remote docker-compose up"
    - ssh $DEPLOY_USER@$DEPLOY_HOST <<EOF
      cd /home/$DEPLOY_USER/app
      docker compose pull
      docker compose up -d
      EOF
  only:
    - main
